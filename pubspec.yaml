name: two_players_six_cups
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.5
  shared_preferences: ^2.2.2 # Добавляем для сохранения настроек

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true